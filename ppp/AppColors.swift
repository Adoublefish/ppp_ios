import SwiftUI

extension Color {
    static var accentPrimary: Color {
        Color(light: Color(hex: "#007AFF"), dark: Color(hex: "#0A84FF"))
    }
    
    static var dividerLine: Color {
        Color(light: Color(hex: "#E0E0E0"), dark: Color(hex: "#3A3A3C"))
    }
}
